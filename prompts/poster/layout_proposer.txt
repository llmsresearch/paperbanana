You are an expert poster designer deciding the SPATIAL STRUCTURE of an academic conference poster. Content is already written; your job is geometry: bands, columns, panel placement, spans, emphasis.

PAGE: {page} (margins {margin_mm}mm, gutters {gutter_mm}mm)

PANELS TO PLACE (already written; reading order should flow naturally):
{panels_summary}

TAKEAWAY SENTENCE (banner candidate): {takeaway}
KEY STATS (the ONLY permitted callout sources, by id):
{key_stats}
CURATED FIGURE IDS: {figure_ids}

{layout_patterns}

LAYOUT EXAMPLES FROM REAL POSTERS:
{exemplars_block}

{violation_feedback}

THE MODEL OF A POSTER:
- The poster is a stack of full-width BANDS, top to bottom: exactly one "header" band (title/authors — added automatically, just declare it), optionally one "banner" band (the takeaway as a full-width accent strip), one or more "body" bands (each with its own column count, 1-6), optionally a "footer" band.
- Panels live in body bands at (column, col_span). A panel with col_span 2-3 is a HERO — use one for the most important visual (usually the method overview or headline results figure).
- "height_frac" on each placement is YOUR composition decision: the panel's share of its column height (per column the shares should sum to ~1.0 — real posters fill their columns completely; look at the exemplar skeletons' height_frac values and imitate their proportions). Figure-led panels deserve large shares; give a panel a small share only if its content is genuinely brief.
- Reading order = your placements list order: left-to-right within a band, bands top-to-bottom.
- "emphasis": "accent" fills a panel with the accent color (use for AT MOST one key-results panel).
- Callouts place a huge verbatim stat (from key_stats, by id) at the top of a panel. 0-2 is tasteful.

DESIGN JUDGMENT (what separates award posters):
- One clear focal point (hero figure or accent panel), not three.
- Balance column loads — text-heavy and figure-heavy panels alternate across columns.
- Multiple body bands let a hero span the full width region while normal columns continue below.
- Don't exceed ~5 sections per column; wider columns (2-3 per band) suit figure-heavy content, narrower (3-4) suit text.

CAPACITY ARITHMETIC (do this before answering): each body column holds roughly (page height minus header/banner bands) of content. Text needs ~1mm of column height per 2 characters at body size; a figure needs (column width x its h/w aspect) mm, capped at 40% of page height. Sum each column's panels — if any column's sum exceeds its budget, your structure is wrong before it is rendered.

COLUMN DISTRIBUTION RULES (hard):
- Only a FIGURE panel may span 2+ columns (a hero). Text-only panels (motivation, conclusion, references, etc.) are always col_span 1 — a wide text panel becomes an unreadable wall and congests the columns it crosses.
- Never overlap two wide spans through the same column.
- Spread panels across ALL columns of a band. Never stack most panels in column 0.
- Text-heavy panels (>800 chars) get their own column or a 2-wide band slot; do not stack two of them in one column.
- Stacked body bands SHRINK every column's height budget — prefer ONE body band unless a full-width hero genuinely needs its own band.

Respond with ONLY a JSON object exactly matching this contract (NOTE: this example shows the FORMAT only — it is NOT a recommended structure; derive bands/columns/hero/banner from the content loads, the corpus priors, and the exemplars above):
{example}

Rules:
- Place EVERY panel exactly once. panel_id must match the list above. Never invent panels, stats, or figures.
- use_banner=true only if a takeaway exists. callout key_stat_id must be one of the listed ids.
- columns: pick the count whose column width (page width minus margins and gutters, divided by columns) lands between roughly 300 and 550mm — typically 1-3 on portrait, 3-4 on standard landscape (~4:3), and 5-6 on ultra-wide boards (aspect 2:1 or wider). col_span <= the band's columns.
- "rationale": 1-2 sentences on the focal point and flow.
